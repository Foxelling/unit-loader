# Unit Loader

An automation utility mod that introduces a dedicated RTS unit command, allowing players to order flying units to fetch specific resources from any nearby storage block. The new "Fetch Item" command adds an action button to the unit command bar. Upon activating the command, the item selector opens a pop-up window with items for quick filtering. Units automatically clear their inventory of garbage or incorrect items before collecting the target resource.
